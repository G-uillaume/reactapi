{
  "homepage": "https://G-uillaume.github.io/reactapi",
  "name": "reactapi",
  "version": "1.0.0",
  "description": "API with React",
  "scripts": {
    "dev": "parcel serve public/index.html",
    "build": "parcel build public/index.html --public-url /reactapi/",
    "predeploy": "rm -rf dist && npm run build",
    "deploy": "gh-pages -d dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "parcel": "^2.0.0-beta.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
